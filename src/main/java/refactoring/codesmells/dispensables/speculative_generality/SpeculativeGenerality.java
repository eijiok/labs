package refactoring.codesmells.dispensables.speculative_generality;

/**
 * Estrutura desnecessariamente muito grande para o futuro que nunca chega
 *
 * Como refatorar:
 * - se fro classes abstratas que não fazem muito, Colapse a Hierarquia
 * - se for delegação desnecessária, Inline Class
 * - se for parametros não usados, Remova Parametro
 *
 */
public class SpeculativeGenerality {
}
